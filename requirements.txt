# Core runtime (install on the cluster GPU node)
torch>=2.1
transformers>=4.44,<5      # <5: transformers 5.x renamed from_pretrained(torch_dtype=)->dtype=, which model_loading.py uses
accelerate>=0.33
peft>=0.12
bitsandbytes>=0.43      # 4-bit / 8-bit quantization (CUDA only)
pyyaml>=6.0
